/**
 * Leaderboard sizing.
 *
 * Players are never shown by a generated handle or any internal id: boards and
 * name tags use the verified Bloxity display name and avatar (`identity.ts`).
 */

/** How many places each board shows. Matches the reference art's eight rows. */
export const LEADERBOARD_SIZE = 8;
